<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(@artist, song) %></li>
  <% end %>
</ul>

<!--Update artists show view to list each song for the artist-->
<!--List each song by iterating over each song, link the song to the tile, and the artist song path-->
<!--Use the new nested resource route HELPER to link each song to show page-->